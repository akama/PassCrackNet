package main

import (
	// "labix.org/?v2/mgo"
	"testing"
	// "io/ioutil"
)

func TestTasksBak(t *testing.T) {
	// const MONGO_URL = "localhost"

	// // Setup the database
	// session, err := mgo.Dial(MONGO_URL)
	// if err != nil {
	// 	panic(err)
	// }

	// defer session.Close()
	// session.SetMode(mgo.Monotonic, true)

	// // Insert data into the database
	// job_file, err := ioutil.ReadFile("input.txt")
	// if err != nil {
	// 	panic(err)
	// }

	// job := createJob(3, 0, job_file, "?a?a?a?a?a?a?a?a", session.Copy())

	// job.createTask(session, 2)
	// //job.createTask(session, 2)

}
